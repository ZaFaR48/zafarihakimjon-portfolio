services:
  portfolio:
    build: .
    container_name: zafarihakimjon-portfolio
    restart: unless-stopped
    ports:
      - "3004:3000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1:3000/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 30s
